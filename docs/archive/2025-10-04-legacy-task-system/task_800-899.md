> NOTE: This is a frozen historical ledger. For active work see `./BACKLOG.md`. For completed items see `./COMPLETED_TASKS.md`.

- [ ] ID: TASK-800
      Title: Tech Debt: Resolve Pylance Type-Checking Warnings
      Owner: @duly3
      Priority: low
      Status: backlog
      Created: 2025-09-14
      Milestone: M7 – Maintenance & Cleanup
      Notes: Address static analysis warnings from Pylance reported in vscode*problems.json. These are non-critical type-checking issues, not runtime bugs, as the test suite is fully green. This task is for improving code hygiene and reducing IDE noise.
      Subtasks: - [ ] In ipam.py, use `cast(Any, ...).in*()`or`col()`to resolve SQLAlchemy instrumentation warnings.

  - [ ] In links.py, status_service.py, and the v2 traffic engine, tighten type hints for function parameters (e.g.,`Status | str | None`) and use `isinstance(x, Status)`checks before accessing the`.value` attribute to satisfy the type checker. - [ ] Ensure the full backend quality gate remains 100% green after applying the fixes.

- [x] ID: TASK-802
      Title: Frontend: Implement Hardware Catalog Viewer and Creation Workflow
      Completed: 2025-09-14
      Milestone: M9 – Foundational Network Emulation
      Commit: <final-git-sha>
      Notes: Implemented the frontend components for port management. Created a new "Hardware" tab with a viewer to list and manage Hardware Models (read-only for now). Critically, the device creation workflow was enhanced: a modal dialog now appears on drag-and-drop, allowing the user to select a specific hardware model for the new device. This ensures that all new devices are created with a proper, catalog-defined port layout. The existing "Quick Add" and "Bulk Create" dialogs were also updated to support hardware model selection.
      Artifacts: unoc-frontend-v2/src/stores/hardwareStore.ts, unoc-frontend-v2/src/components/pages/HardwarePage.vue, unoc-frontend-v2/src/composables/topologyCore/drop.ts, unoc-frontend-v2/src/components/layout/TopologyCanvas.vue

- [x] ID: TASK-803
      Title: Frontend: Visualize Port Occupancy in Details Panel
      Completed: 2025-09-14
      Milestone: M9 – Foundational Network Emulation
      Commit: <final-git-sha>
      Notes: Completed the port management feature by adding a "Ports" section to the Device Details panel. When an OLT or AON Switch is selected, this section now fetches and displays live port occupancy data from the `/api/ports/summary` endpoint. The UI visualizes role-specific data, such as `[10 / 64]` for PON ports and `[1 / 1]` for Access ports, and color-codes each entry based on its `effective_status`. A lightweight polling mechanism ensures the data remains up-to-date.
      Artifacts: unoc-frontend-v2/src/components/layout/DetailsPanel.vue
