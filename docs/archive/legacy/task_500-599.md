- [x] ID: TASK-557
      Title: Critical: Enforce Topology-Aware Provisioning (strict by default)
      Completed: 2025-09-14
      Milestone: M8 – Provisioning Stability
      Commit: <final-git-sha>
      Notes: Flipped system default to strict provisioning. The dependency resolver and status service are now fully state-aware, validating upstream paths against "viable" links (admin_status='UP') and provisioned anchors. A break in this path post-provisioning correctly transitions a device's status to DEGRADED. The TrafficEngineV2 was refactored to be a simple consumer of this authoritative status, ensuring traffic generation ceases for DEGRADED devices. This change significantly enhances emulation realism and system stability.
      Artifacts: backend/services/dependency_resolver.py, backend/services/status_service.py, backend/services/v2_engine.py, backend/tests/test_provisioning_strict_dependencies.py, backend/tests/test_status_lifecycle.py

- [x] ID: TASK-563
      Title: Bugfix: Ensure Link 'Force Down' is Effective System-Wide
      Completed: 2025-09-14
      Milestone: M8 – Provisioning Stability
      Commit: <final-git-sha>
      Notes: Fixed a critical bug where a link's 'Force Down' admin override was not respected by the dependency resolver and the traffic engine. Implemented a centralized `is_link_passable` helper in `status_service` to act as the single source of truth for link viability. This helper was integrated into both the dependency resolver and the v2 traffic engine. This ensures that forcing a link down correctly triggers a `DEGRADED` status on dependent active devices and halts all traffic flow over that link. The `LinkOut` API schema was also updated with an `effective_status` field for clear and unambiguous UI representation.
      Artifacts: backend/services/status_service.py, backend/services/dependency_resolver.py, backend/services/v2_engine.py, backend/api/schemas.py, backend/api/endpoints/links.py, backend/tests/test_link_force_down_effects.py

## Epic E3 – Hardware Catalog & Device Realization

- TASK-521: Backend: Implement Hardware Catalog Models & Loader

  - Status: IN_PROGRESS
  - Milestone: M9 – Foundational Network Emulation
  - Notes: HardwareModel, PortProfile, idempotent JSON loader, basic APIs (list/get)

- TASK-522: Backend: Integrate Hardware Catalog into Device Lifecycle

  - Status: TODO
  - Priority: high
  - Milestone: M9 – Foundational Network Emulation
  - Summary: Link devices to HardwareModel and auto-create Interfaces from PortProfiles on device creation

- TASK-523: Backend: Derive Device Parameters from Hardware Catalog - Status: DONE - Priority: high - Milestone: M9 – Foundational Network Emulation - Summary: Use catalog defaults for optical and capacity with override mechanism; enrich device API

## Epic E4 – L2 Switching Pipeline (Minimal)

- TASK-531: Backend: Bridge-Domain and MAC Learning Models

      - Status: IN_PROGRESS
      - Milestone: M10 – L2 Foundations
      - Notes: Introduce BridgeDomain and MacAddressEntry models, extend Interface with bridge_domain_id, add MAC table API and tests.

- TASK-533: Backend: Auto-create Default BridgeDomain for Switches

      - Status: IN_PROGRESS
      - Priority: medium
      - Milestone: M9 – Foundational Network Emulation
      - Notes: On POST /api/devices for switch-like types (e.g., AON_SWITCH), auto-create a default BridgeDomain (name: "default") for the device. Tests updated to remove manual BD creation and assert it exists automatically.

## Epic E5 – L3 VRF & Routing (Static)

- TASK-541: Backend: Implement VRF, RIB, and Static Route Models

      - Status: IN_PROGRESS
      - Priority: high
      - Milestone: M9 – Foundational Network Emulation
      - Notes: Introduce Route (RIB) and Neighbor (ARP/Neighbor cache) models; add routing API to add/list static routes per VRF under device context.

- [ ] ID: TASK-511
      Title: Backend: Implement Prefix & VRF Models
      Owner: @duly3
      Priority: high
      Status: in_progress
      Created: 2025-09-10
      Milestone: M9 – Foundational Network Emulation
      Notes: E2.1 – Introduce VRF and Prefix models; make InterfaceAddress allocations prefix-aware; provide CRUD APIs.
      Subtasks: - [ ] VRF SQLModel (unique name) + CRUD API - [ ] Prefix SQLModel (cidr, vrf_id, description) + CRUD API - [ ] Device: default vrf_id field - [ ] InterfaceAddress: prefix_id and allocation logic - [ ] Update interfaces address POST API to accept prefix_id - [ ] Tests for models and endpoints

- [ ] ID: TASK-562
      Title: Backend: Physical Medium mapping for infrastructure links
      Owner: @duly3
      Priority: high
      Status: in_progress
      Created: 2025-09-13
      Milestone: M9 – Foundational Network Emulation
      Notes: Extend link rules to explicitly cover infra uplinks and tighten allowed media. Added backend tests; frontend verification pending.
      Subtasks: - [x] Add infra uplink classes (access_uplink_aon, access_uplink_olt) - [x] Restrict routed_p2p to SMF-only - [x] Map allowed media: access_uplink_olt=SMF-only; access_uplink_aon=SMF/MMF/Copper; optical_segment/termination=SMF/MMF; access_edge=Copper - [x] Backend unit tests for Core↔Edge and OLT↔Edge allowed-media - [ ] Frontend manual verification (Link Details dropdown) - [ ] Frontend unit tests for allowed-media binding (optional)

- [ ] ID: TASK-553
      Title: Tests: Add Mixed L2/L3 Multi-Hop Forwarding Scenarios
      Owner: @duly3
      Priority: high
      Status: active
      Created: 2025-09-10
      Milestone: M9 – Foundational Network Emulation
      Notes: Add tests covering End Device → Switch → Router → Destination with L2 flood/forward then L3 route; assert resolved path correctness and include per-hop metadata.
      Subtasks: - [ ] Extend resolve_flow_path hop metadata (action, device_type, link_id) - [ ] New test topology with End Device, Switch, Router, Dest - [ ] Assertions on L2 then L3 actions sequence - [ ] Validate final device/interface and hop count

- [ ] ID: TASK-557
      Title: Critical: Enforce Topology-Aware Provisioning (strict by default)
      Owner: @duly3
      Priority: critical
      Status: active
      Created: 2025-09-12
      Milestone: M8 – Provisioning Stability
      Notes: Current provisioning allows devices to be provisioned without validating upstream connectivity. Flip defaults and enforce path validation across device classes.
      Subtasks: - [ ] Flip defaults: constants/provisioning.py -> set ALLOW_RELAXED_UPSTREAM_CHECK = False - [ ] Path dependency on by default: services/dependency_resolver.py -> USE_PATH_VALIDATION default True; keep PATH_RELAXED opt-in via env - [ ] Extend evaluate_upstream_dependencies: - [ ] CORE_ROUTER -> must have logical path to BACKBONE_GATEWAY - [ ] EDGE_ROUTER -> must have logical path to CORE_ROUTER - [ ] OLT -> must have logical path to CORE_ROUTER (already exists, re-verify) - [ ] AON_SWITCH -> must have logical path to CORE_ROUTER (add) - [ ] ONT/BUSINESS_ONT -> must have optical path to OLT (exists) - [ ] provisioning_service: always use dependency_resolver path checks when available; fallback presence-check only when explicitly disabled - [ ] Error semantics: if dependency check fails -> raise ErrorCode.INVALID_PROVISION_PATH with a clear reason suffix (e.g., "no upstream core reachable") - [ ] Unit tests: add failure and success cases for each device class (no link -> 400, with link -> 200). Ensure env defaults (strict) are used in tests.
- [ ] ID: TASK-558
      Title: Refactor: Seed and Dev Reset should not pre-provision or assign IPs
      Owner: @duly3
      Priority: high
      Status: active
      Created: 2025-09-12
      Milestone: M8 – Provisioning Stability
      Notes: Align seed with new strict provisioning. Create clean, unprovisioned devices only; optionally provide a demo topology with proper links but still unprovisioned. Keep IPAM defaults idempotent.
      Subtasks: - [ ] seed_service.ensure_backbone_gateway: keep as anchor (provisioned=True), but update docstring to reflect default flag behavior; do not assign mgmt IP unless ALLOW_BACKBONE_MGMT_IP=1 - [ ] reset_dev_db.py --seed: insert POP, CORE, EDGE, OLT as unprovisioned and add links only when a new --demo-topology flag is provided; never call provisioning - [ ] Ensure ensure_ipam_defaults is used only to create VRFs/Prefixes; no IP allocation occurs outside provisioning_service - [ ] Update tests referencing seed to avoid assumptions about preassigned IPs

- [ ] ID: TASK-559
      Title: Tests & API contracts for INVALID_PROVISION_PATH
      Owner: @duly3
      Priority: high
      Status: active
      Created: 2025-09-12
      Milestone: M8 – Provisioning Stability
      Notes: Strengthen test matrix and document API error responses.
      Subtasks: - [ ] Add tests: core w/o backbone link, edge w/o core link, olt w/o core link, ont w/o olt optical path - [ ] Verify response: HTTP 400 with detail starting "INVALID_PROVISION_PATH" and reason suffix - [ ] Update API docs/schema notes to reflect strict default and opt-in relax modes via env

## TASK-513A — Bugfix: Correct Default IPAM Prefixes in Seed

- Status: completed
- Priority: high
- Milestone: M9 – Foundational Network Emulation
- Owner: Backend Platform
- Summary: Ensure the default management IPAM seed matches architecture. Replace legacy lan/wan dev pools with architecture-compliant management pools and align POOL_DEFS across modules to 10.250.x ranges. Keep idempotent and non-destructive behavior for existing test fixtures.

### Scope

- Refactor `ensure_ipam_defaults` (no change in behavior if already aligned) to guarantee the following mgmt VRF prefixes exist (one per description):
  - core_mgmt → 10.250.0.0/24
  - ont_mgmt → 10.250.1.0/24
  - aon_mgmt → 10.250.2.0/24
  - cpe_mgmt → 10.250.3.0/24
  - olt_mgmt → 10.250.4.0/24
  - noc_tools → 10.250.10.0/24
- Update `POOL_DEFS` in constants and provisioning modules to architecture values:
  - core_mgmt → 10.250.0.0/24
  - access_mgmt → 10.250.4.0/24 (covers OLT and AON access domain)
  - ont_mgmt → 10.250.1.0/24
  - cpe_mgmt → 10.250.3.0/24
- Replace IPAM endpoint dev auto-seed (lan/wan) with management pools using `POOL_DEFS`.

### Acceptance Criteria

- GET `/api/ipam/pools` (with UNOC_DEV_FEATURES=1) auto-seeds and returns only management pools consistent with architecture CIDRs. No lan/wan entries appear.
- Seeding is idempotent and coexists with tests that insert custom pools/prefixes; no duplicates/conflicts occur.
- Full backend test suite remains green.

### Verification

- Reset DB and seed minimal demo, then visit `/api/ipam/pools` and confirm pool keys and CIDRs match architecture.
- Manual UI check: IPAM tab lists the management pools; utilization counters work.

Verification results (2025-09-13):

- Live API `GET /api/ipam/pools` returned only management pools with expected CIDRs:
  - core_mgmt 10.250.0.0/24, access_mgmt 10.250.4.0/24, ont_mgmt 10.250.1.0/24, cpe_mgmt 10.250.3.0/24.
- Full backend test suite passed (188 tests, 1 warning).

## TASK-554E — Bugfix: Simulation & Cockpit Polish

- Status: in_progress
- Priority: critical
- Milestone: M9 – Foundational Network Emulation
- Owner: Platform (Backend + Frontend)
- Summary: Fix simulation gating so offline ONTs (e.g., NO_SIGNAL) never generate traffic in TrafficEngineV2 and polish cockpit UI layouts to prevent overlapping text.

### Scope

- TrafficEngineV2: Effective status gating
  - At the start of each tick, determine device effective status via `status_service.evaluate_device_status` and only generate for leaves that are effectively UP.
  - Guarantee ONTs with `signal_status=NO_SIGNAL` are treated as DOWN (per status_service) and skipped during generation and aggregation.
  - Ensure `deviceMetricsUpdated` never includes devices that were skipped.
- UI Cockpits: Overlap-free layout
  - Refactor cockpit components to use a flexible layout (foreignObject + CSS Flexbox) for name/status lines inside the cockpit frame, preventing overlaps with multi-line content.
  - Maintain clear spacing for utilization/throughput lines in Router/ONT cockpits.

### Acceptance Criteria

- Backend
  - Given an ONT with a tariff, `provisioned=true`, and `signal_status=NO_SIGNAL`, after a tick:
    - No traffic is generated for that ONT (zero aggregation, not present in `deviceMetricsUpdated`).
  - New test added passes consistently and full backend test suite remains green.
- Frontend
  - Cockpit text is clean, non-overlapping, and aligned across devices (Router, ONT, etc.), verified manually in the browser.

### Verification

- Add test `backend/tests/test_traffic_offline_ont_zero.py` that constructs an ONT with failed signal, runs one tick, and asserts no deviceMetricsUpdated entry for it (and aggregates remain zero).
- Manually verify cockpits in the UI after rebuild/reload; long names wrap or ellipsize without overlapping other metrics.

---

## TASK-554C — Traffic aggregation and capacity defaults

- Status: in_progress
- Priority: Critical (P0)
- Milestone: M9
- Owner: backend
- Summary: Fix incomplete upstream traffic aggregation in the v2 traffic engine and add effective device capacity defaults used when no override or hardware model capacity is defined. Update debug snapshot to expose effective capacities. Add end-to-end tests to validate full-path aggregation and non-null capacities.

## TASK-554B — Bugfix: Enable and Verify TrafficEngineV2 Generation

- Status: in_progress
- Priority: critical
- Milestone: M9 – Foundational Network Emulation
- Owner: Backend Platform
- Summary:
  TrafficEngineV2 (TariffTrafficRunner) is constructed but disabled by default due to the `TRAFFIC_ENABLED` flag defaulting to false. As a result, no periodic traffic is generated even when devices are online with tariffs. Flip the default to enabled, add startup/debug logging, and verify end-to-end (events + snapshot + frontend behavior).

## TASK-554D — Bugfix: Finalize Traffic Engine Pathing and Capacity

- Status: in_progress
- Priority: Critical (P0)
- Milestone: M9 – Foundational Network Emulation
- Owner: backend
- Summary: Prefer real, physical paths in the traffic engine by using weighted shortest-path (real edges cost=1, synthetic cost=10). Add interface role-based capacity fallbacks (access=1000 Mbps, p2p_uplink=10000 Mbps, management=1000 Mbps) to ensure finite per-port utilization without PortProfiles.

  ## TASK-562 — Enhancement: Complete Physical Medium Mapping for All Link Types

  - Status: in_progress
  - Priority: high
  - Milestone: M9 – Foundational Network Emulation
  - Owner: Platform (Backend + Frontend)
  - Summary: Extend context-aware Physical Medium mapping beyond access links to include backbone and distribution links. Populate the "Physical Medium" dropdown for infrastructure links (Core↔Edge, OLT↔Edge, AON Switch↔Edge) by defining allowed media for routed_p2p and access uplink variants.
